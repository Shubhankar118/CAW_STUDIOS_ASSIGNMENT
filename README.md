# CAW_STUDIOS_ASSIGNMENT

BaseFile contains the Before And After method to initialize and close the browser.
Inside resources Config.properties contains URL of the website.
testdata.Json contains the JSON data.
Inside testCase contains TableHTML.java file. In this file BASETEST.JAVA is extended. 
TableHTML contains all the Function, XATH and code to run the test and assert the JSON data inside a TRY CATCH block. 
